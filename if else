/*A leap year has 366 days. Write a program to find out 
if a year is a leap year or not. */

Integer year = 2000;
if (Math.mod(year, 4) == 0) {
    if(Math.mod(year, 100) == 0){
        if (Math.mod(year, 400) == 0) {
            System.debug('It is a leap year');
                } else {
                    System.debug('It is not a leap year');
                }
    } else {
        System.debug('It is a leap year');
    }
}   else {
    System.('It is not a leap year');
} 
